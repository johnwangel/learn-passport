const Users = require('./models/users');
// require packages

// invoke express

// add middleware

// add routes
// app.get('/', (req, res) => {
//   res.send('smoke test');
// });

module.exports = app;

// custom authentication check middleware
